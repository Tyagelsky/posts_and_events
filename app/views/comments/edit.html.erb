<h1>Edit comment</h1>
<%= form_for @comment, url: post_comment_path({post_id: @post.id})  do |f| %>
  <div class="field">
    <%= f.text_area :body %>
  </div>
  <%= f.submit "OK", class: "btn btn-large btn-primary" %>
<% end %>
